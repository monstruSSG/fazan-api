const expect = require('chai').expect;

describe('example()', function() {
    context('example context', function() {
        it('should return true as example', function() {
            expect(true).to.equal(true);
        });
    });
});